import { FilterLengthPipe } from './filter-length.pipe';

describe('FilterLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
